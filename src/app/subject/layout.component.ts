import { Component } from "@angular/core";
import { RouterModule } from "@angular/router";

@Component({ standalone: true, templateUrl: 'layout.component.html', imports: [RouterModule]})
export class LayoutComponent{
  
}