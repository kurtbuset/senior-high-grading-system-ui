import { Component } from "@angular/core";
import { RouterModule } from "@angular/router";

@Component({ selector: 'admin', templateUrl: 'layout.component.html', standalone: true,
imports: [RouterModule] })
export class LayoutComponent{}